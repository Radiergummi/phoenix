'use strict';

/*
 global module,
 require
 */
const Element = require( './Element' );

/**
 * Provides a section element. Sections group nodes that belong together. You could also call it a
 * container element.
 *
 * @class
 * @extends Element
 * @extends Node
 */
class Section extends Element {
}

/**
 * @type {Section}
 */
module.exports = Section;
